services:
  mongodb:
    image: mongo:7
    container_name: attendance-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: attendance-system

volumes:
  mongo-data:
